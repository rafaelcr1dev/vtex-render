// ***************************
// To promises
// ***************************
// import '@babel/polyfill';

import './app/index.js';

(function (document, window, $) {
  const $doc = $(document);
  const $win = $(window);

  $doc.ready(function () {});

  $win.load(function () {});
})(document, window, jQuery);
